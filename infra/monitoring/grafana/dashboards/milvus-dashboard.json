{
  "annotations": {"list": []},
  "description": "Milvus cluster overview dashboard (basic)",
  "panels": [
    {
      "type": "graph",
      "title": "Milvus Query QPS",
      "targets": [
        {"expr": "sum(rate(milvus_search_requests_total[1m]))", "refId": "A"}
      ],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "graph",
      "title": "Milvus Query Latency (p95)",
      "targets": [
        {"expr": "histogram_quantile(0.95, sum(rate(milvus_search_duration_seconds_bucket[5m])) by (le))", "refId": "A"}
      ],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    }
  ],
  "schemaVersion": 27,
  "title": "Milvus Overview",
  "version": 1
}